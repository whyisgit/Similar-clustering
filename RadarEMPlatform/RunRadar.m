% 课题3 - 多体制雷达电磁信号平台 启动脚本
% 在MATLAB中运行本脚本以启动应用。
% 说明：此脚本仅负责将当前目录加入搜索路径，并调用主App的运行入口。

% 获取当前脚本的完整路径，并将其所在目录加入到MATLAB搜索路径中，确保可以找到同目录下的类与函数
addpath(fileparts(mfilename('fullpath'))); % 将当前文件夹加入路径

% 调用应用入口，创建UI并初始化默认信号
RadarApp.run();